[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
models = { path = "../models" }
parser = { path = "../parser" }
html = { path = "../html" }
toml = "1.0.6"
serde = { version = "1.0.228", features = ["derive"] }
